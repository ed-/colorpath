Generates a gradient-colored string containing your pwd.

Usage: export PS1=`$(~/bin/colorpath) \$` or some such in your .profile or .bashrc

Feeds from two environment variables, PS1_COLOR1 and PS1_COLOR2 to generate
a gradient.

Have fun!
